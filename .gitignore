# ===================================================================
# General Dependencies
# ===================================================================
# The node_modules folder contains all of your project's installed
# dependencies. It can be massive and should always be ignored.
# It will be generated automatically when you run pnpm install.
node_modules
pnpm-lock.yaml

# ===================================================================
# Build & Output Directories
# ===================================================================
# These folders are generated by the TypeScript compiler or other
# build tools. They contain the compiled JavaScript code that will
# be run, not the source code you write.
dist
build
.next
.out

# ===================================================================
# Monorepo Tooling (Turborepo)
# ===================================================================
# Turborepo creates a .turbo directory to store its cache. This
# is what makes your builds fast, but it doesn't need to be
# committed to version control.
.turbo

# ===================================================================
# Supabase
# ===================================================================
# The supabase directory is often generated locally by the Supabase
# CLI and can contain sensitive information or local-only state.
# Your migrations, which are in supabase/migrations, SHOULD be
# committed, so we explicitly un-ignore them.
supabase/
!supabase/migrations

# ===================================================================
# Environment Variables & Secrets
# ===================================================================
# NEVER commit your environment variables. They contain secret keys
# for your database, APIs, etc. Create a .env.example file to
# show what variables are needed, but the .env file itself
# must be ignored.
.env
.env.*
!.env.example

# ===================================================================
# Logs & Debugging
# ===================================================================
# Log files generated during development or testing.
.log
npm-debug.log
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ===================================================================
# Operating System & IDE Files
# ===================================================================
# Files generated by your operating system or code editor.
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db

# IDEs
.vscode/
.idea/
*.suo
.nuspec
*.njsproj
*.sln
*.sw?